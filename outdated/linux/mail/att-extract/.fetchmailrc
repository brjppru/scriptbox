defaults
        no dns
        mimedecode
        fetchall

poll imap-mail.outlook.com proto imap auth password user 'ya@outlook.com' pass 'yaya' ssl keep folder 'Inbox'

mda "/usr/bin/procmail -d %T"
